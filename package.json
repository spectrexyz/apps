{
  "name": "spectre-apps",
  "private": true,
  "scripts": {
    "kit:build": "cd pkg/kit && yarn build:all",
    "kit-legacy:build": "cd pkg/kit-legacy && yarn build",
    "website:build": "yarn kit-legacy:build && cd pkg/website && yarn copy-kit-assets && yarn build"
  },
  "workspaces": {
    "packages": [
      "pkg/*"
    ]
  }
}
