{
  "name": "spectre-apps",
  "private": true,
  "scripts": {
    "kit-legacy:build": "cd pkg/kit-legacy && yarn build",
    "website:build": "yarn kit-legacy:build && cd pkg/website && yarn kit-legacy-assets && yarn build"
  },
  "workspaces": {
    "packages": [
      "pkg/*"
    ]
  }
}
